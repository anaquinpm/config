# Build stage
FROM golang:1.21 AS builder
WORKDIR /app
COPY . .
RUN go build -o app .

# Runtime stage
FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/app .
EXPOSE 8080
CMD ["./app"]
